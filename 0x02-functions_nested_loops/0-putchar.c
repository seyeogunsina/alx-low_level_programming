#include <stdio.h>
#include <string.h>

/**
 * main - prints _putchar
 * Return: returns number 0
 */

int main(void)
{
	char str_o[] = "_putchar";
	int i;
	int len = strlen(str_o);

	for (i = 0; i <= len; i++)
	{
		putchar(str_o[i];
	}

	return (0);
}
